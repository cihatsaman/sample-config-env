# Sample Configuration of Environments with TS
 > Use `dotenv` in a simple TypeScript project. 
 ---

## Getting Started

1. install the packages with `npm install` 
2. Run the `watch-ts`
3. Run the `watch-node`
